#include <stdio.h>#include <sys/types.h>#include <sys/stat.h>#include <fcntl.h>#include <unistd.h>int main(){	char buf[32] = {0};	int fd = open("/dev/chardev",O_RDWR);	if(fd == -1)	{		printf("open failed");		return -1;	}	read(fd,buf,sizeof(buf));	write(fd,buf,sizeof(buf));	close(fd);	return 0;}